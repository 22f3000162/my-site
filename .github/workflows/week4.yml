name: Sheduler
on:
  schedule:
    - cron: '48 12 * * *'
jobs:
  issue:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: 22f3000162@ds.study.iitm.ac.in
        run: |
          echo "Daily commit" >> daily-log.txt
          git add daily-log.txt
          git commit -m "Daily commit"
          git push
